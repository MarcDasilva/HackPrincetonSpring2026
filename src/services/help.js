console.log(`
OpenClaw Minecraft Orchestrator

Core commands:
  npm run photon          macOS Photon/iMessage bridge
  npm run foreman         control-plane foreman service
  npm run worker:miner    worker VM runtime for miner
  npm run worker:builder  worker VM runtime for builder
  npm run worker:forager  worker VM runtime for forager
  npm run simulation      run local seeded demo

State model:
  world_objects, agent_status, chat_messages, jobs_history, agent_memory
`);
