# Simulation

Run:

```bash
npm run simulation
npm run simulation:verify
```

Simulation uses `.simulation/openclaw-state.json` and fake OpenClaw/Voyager. It validates the control flow without real Supabase, iMessage, Dedalus, Minecraft, or Voyager credentials.
